#!/bin/sh
set -eu

go run ./cmd/publiccheck --repo . --staged

if ! command -v gitleaks >/dev/null 2>&1; then
  echo "gitleaks is required; install it before committing" >&2
  exit 1
fi

gitleaks protect --staged --redact
